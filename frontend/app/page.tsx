"use client";

// Dashboard placeholder — Task 6 fills this in with the "what needs you"
// bands from lib/dashboard.ts. VoiceProvider/ConversationRail moved to
// app/layout.tsx so they survive navigation to the other seven views.
export default function Page() {
  return <h2 className="viewtitle">Dashboard</h2>;
}
