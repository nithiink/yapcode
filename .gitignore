# Secrets — never commit
.env
.env.*
!.env.example

# Python
backend/.venv/
__pycache__/
*.pyc

# Node / Next
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/tsconfig.tsbuildinfo
frontend/.certs/

# Interactive CLI session control dirs (runtime storage rehydration reads).
# Default location is inside the project; override with VC_SESSION_STORE.
.voice-claude/

# Cost log (per-connection voice + Claude session cost JSONL; for analysis,
# not source). Override path with VC_COST_LOG_PATH.
cost-log.jsonl

# Misc
.DS_Store
